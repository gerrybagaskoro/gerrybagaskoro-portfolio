<script lang="ts">
  // This component might be redundant if tech stack is included in experience, 
  // but the screenshot shows "Technologies used" under each experience.
  // If there's a global tech stack section, we can add it here.
  // For now, I'll leave it simple or empty if not needed globally.
</script>
